H2 SELECT ALL (Inner Join): SELECT c.name as "Name", c.email as "E-mail", a.account_number as "Number", a.account_type as "Type", a.branch_address as "Address", a.created_at as "Created at", a.created_by as "Created by" FROM customers c INNER JOIN accounts a ON c.customer_id = a.customer_id